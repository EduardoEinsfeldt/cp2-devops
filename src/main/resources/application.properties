spring.application.name=dimdim-crud
spring:
    datasource:
        url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:dimdim}
        username: ${DB_USER:root}
        password: ${DB_PASSWORD:senha123}
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
